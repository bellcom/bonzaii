  {% if highlight %}
    <ul>
      <li><h3>{{ headline|default('Standart løsninger') }}</h3></li>
    {% for item in highlight %}
      <li>
        <span>{{ item.date }}</span>
        <a href="/article/{{ item.slug }}">{{ item.title }}</a>
      </li>
    {% endfor %}
    </ul>
  {% endif %}
